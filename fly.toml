# fly.toml app configuration file generated for yuzuki-md-v2 on 2026-06-04T12:27:23Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'yuzuki-md-v2'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[env]
  LOG_LEVEL = 'info'
  PORT = '3000'

[[mounts]]
  source = 'yuzuki_session'
  destination = '/app/bot_session'

[[mounts]]
  source = 'yuzuki_data'
  destination = '/app/data'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
