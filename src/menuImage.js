/**
 * DEPRECATED — menuImage.js
 *
 * Canvas-based menu image generator. No longer imported by any module.
 * Superseded by the NativeFlow carousel (src/nativeflow/carousel.js)
 * which produces fully interactive WhatsApp cards without requiring
 * a canvas library or image generation.
 *
 * Kept as a tombstone to preserve git history.
 * Safe to delete in a future cleanup pass.
 */
